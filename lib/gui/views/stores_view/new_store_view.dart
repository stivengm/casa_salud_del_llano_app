import 'package:flutter/material.dart';

class NewStoreView extends StatelessWidget {
  const NewStoreView({ Key? key }) : super( key: key );

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}